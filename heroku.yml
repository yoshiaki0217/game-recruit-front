setup:
  config:
    NODE_ENV: production
    SKIP_PREFLIGHT_CHECK: true
    REACT_APP_BACKEND_PATH: https://game-recruit-api.herokuapp.com
    REACT_APP_PUSHER_APP_KEY: 12eaa957c8aac9f71905
    REACT_APP_PUSHER_APP_CLUSTER: ap3
build:
  docker:
    web: Dockerfile
  config:
    WORKDIR: app
    API_URL: http://https://game-recruit-api.herokuapp.com/
run:
  web: npm start